{
  "customizations": {
    "vscode": {
      "extensions": [
        "rvest.vs-code-prettier-eslint",
        "github.codespaces",
        "github.copilot",
        "github.copilot-chat",
        "ms-azuretools.vscode-docker"
      ]
    }
  },

  "features": {
    "ghcr.io/devcontainers/features/desktop-lite:1": {
      "version": "latest",
      "noVncVersion": "1.2.0",
      "password": "p",
      "webPort": "6080",
      "vncPort": "5901"
    }
    // "ghcr.io/devcontainers/features/common-utils:2": {"username":"user1"}
  },

  "forwardPorts": [5901, 6080],

  // "build": { "dockerfile": "Dockerfile" },
  // "image": "cimg/node:18.19-browsers",
  "image": "mcr.microsoft.com/devcontainers/universal:2-linux",

  "name": "MetaMask Codespace for Extension Development",

  "portsAttributes": {
    "5901": {
      "label": "local vnc"
    },
    "6080": {
      "label": "noVNC web"
    }
  }

  // "postCreateCommand": ".devcontainer/start-xvfb.sh"
  // "postCreateCommand": "sudo apt-get update && export DEBIAN_FRONTEND=noninteractive && curl -sSL https://dl.google.com/linux/direct/google-chrome-stable_current_$(dpkg --print-architecture).deb -o /tmp/chrome.deb && sudo apt-get -y install /tmp/chrome.deb"
}
